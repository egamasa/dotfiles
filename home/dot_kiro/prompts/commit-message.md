Git でステージングされている変更に対するコミットメッセージを作成してください。

手順:
1. `git diff --cached --stat` でステージングされたファイルを確認する
2. `git diff --cached` で変更内容の詳細を確認する
3. `git log --oneline -20` で過去のコミットメッセージのスタイル（言語、形式、接頭辞の有無など）を確認する
4. 過去のスタイルに合わせたコミットメッセージを提案する

ステージングされた変更がない場合はその旨を伝えてください。
